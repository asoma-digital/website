import { Mail } from 'lucide-react';

export default function Icon () {
    return (
        <div className="icon" style={{background: 'linear-gradient(90deg, #0486BE 0%, #04BDE7 100%)',}}>
            <Mail size={28} color="#FFFFFF" strokeWidth={2}/>
        </div>
    )
}